# THIS FILE IS GENERATED BY OpenApiSchemaExportTest — DO NOT EDIT MANUALLY.
# Run `./mvnw -pl service test -Dtest=OpenApiSchemaExportTest` to regenerate.
---
openapi: 3.1.0
components:
  schemas:
    ActiveProcessResponse:
      type: object
      properties:
        technicalProcessId:
          type: string
    AddReleaseRequestSource:
      type: object
      required:
      - branch
      properties:
        branch:
          type: string
          pattern: \S
        requester:
          type: string
        priority:
          type: string
    AdoptProjectRepositoryRequest:
      type: object
      required:
      - repositoryId
      - name
      - archetype
      properties:
        repositoryId:
          type: string
          pattern: \S
        name:
          type: string
          pattern: \S
        url:
          type: string
        archetype:
          $ref: "#/components/schemas/RepositoryArchetype"
    AgentContainerResponse:
      type: object
      properties:
        container:
          $ref: "#/components/schemas/ContainerView"
          type: object
          description: "The project's agent container, as this service last observed\
            \ it."
    AgentRuntimeStatus:
      type: string
      enum:
      - RUNNING
      - STOPPED
      - PROVISIONING
      - FAILED
      - ABSENT
    AttachmentDto:
      type: object
      properties:
        id:
          type: string
        mimeType:
          type: string
        label:
          type: string
        source:
          type: string
        createdAt:
          $ref: "#/components/schemas/Instant"
        dataBase64:
          type: string
    AuditEntityType:
      type: string
      enum:
      - EPIC
      - FEATURE
      - TASK
      - TICKET
      - TICKET_COMMENT
    AuditEntryDto:
      type: object
      properties:
        id:
          type: string
        entityType:
          $ref: "#/components/schemas/AuditEntityType"
        entityId:
          type: string
        epicId:
          type: string
        operation:
          $ref: "#/components/schemas/AuditOperation"
        changedBy:
          type: string
        changedAt:
          $ref: "#/components/schemas/Instant"
        snapshot:
          type: string
    AuditOperation:
      type: string
      enum:
      - CREATE
      - UPDATE
      - DELETE
    BackupOutcome:
      type: string
      enum:
      - SUCCEEDED
      - AUTH_REQUIRED
      - UNREACHABLE
      - FAILED
    BranchDto:
      type: object
      properties:
        name:
          type: string
        canCleanup:
          type: boolean
        parent:
          type: string
        ahead:
          type: integer
          format: int32
        behind:
          type: integer
          format: int32
    CommitBuildStatusDto:
      type: object
      properties:
        runId:
          type: string
        status:
          type: string
        branch:
          type: string
        gating:
          type: boolean
        finishedAt:
          $ref: "#/components/schemas/Instant"
    CommitChangesDto:
      type: object
      properties:
        commit:
          type: string
        parent:
          type: string
        files:
          type: array
          items:
            $ref: "#/components/schemas/CommitFileChangeDto"
    CommitDto:
      type: object
      properties:
        hash:
          type: string
        shortHash:
          type: string
        author:
          type: string
        email:
          type: string
        date:
          type: string
        message:
          type: string
        files:
          type: array
          items:
            type: string
    CommitFileChangeDto:
      type: object
      properties:
        path:
          type: string
        oldPath:
          type: string
        changeType:
          type: string
    CommitFileDiffDto:
      type: object
      properties:
        path:
          type: string
        changeType:
          type: string
        diff:
          type: string
    CommitLogDto:
      type: object
      properties:
        branch:
          type: string
        parent:
          type: string
        commits:
          type: array
          items:
            $ref: "#/components/schemas/CommitDto"
    ConflictedPath:
      type: object
      properties:
        path:
          type: string
        head:
          type: string
        headSha:
          type: string
        reason:
          type: string
    ContainerView:
      type: object
      properties:
        runtimeStatus:
          $ref: "#/components/schemas/AgentRuntimeStatus"
        daemonConnected:
          type: boolean
        daemonVersion:
          type: string
        failureDetail:
          type: string
    CreateEpicRequest:
      type: object
      required:
      - title
      properties:
        title:
          type: string
          pattern: \S
        description:
          type: string
    CreateFeatureRequest:
      type: object
      required:
      - title
      properties:
        title:
          type: string
          pattern: \S
        description:
          type: string
        dependsOnFeatureId:
          type: string
    CreateProjectRepositoryRequest:
      type: object
      properties:
        url:
          type: string
        name:
          type: string
        archetype:
          $ref: "#/components/schemas/RepositoryArchetype"
        component:
          type: string
    CreateProjectRequest:
      type: object
      required:
      - name
      - dns
      properties:
        name:
          type: string
          pattern: \S
        slug:
          type: string
        description:
          type: string
        url:
          type: string
        dns:
          $ref: "#/components/schemas/DnsSpec"
    CreateReleaseRequest:
      type: object
      required:
      - branch
      - summary
      properties:
        branch:
          type: string
          pattern: \S
        summary:
          type: string
          pattern: \S
        requester:
          type: string
        priority:
          type: string
    CreateTaskRequest:
      type: object
      required:
      - repositoryId
      - title
      properties:
        repositoryId:
          type: string
          pattern: \S
        title:
          type: string
          pattern: \S
        description:
          type: string
        dependsOnTaskId:
          type: string
    CreateTicketCommentRequest:
      type: object
      required:
      - body
      properties:
        body:
          type: string
          pattern: \S
    CreateTicketRequest:
      type: object
      required:
      - title
      - type
      properties:
        title:
          type: string
          pattern: \S
        description:
          type: string
        type:
          type: string
          pattern: \S
        assignee:
          type: string
    DesignDto:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        status:
          type: string
        basedOnDesignId:
          type: string
        note:
          type: string
        sourceRoute:
          type: string
        htmlBytes:
          type: integer
          format: int32
        truncated:
          type: boolean
        createdBy:
          type: string
        createdAt:
          $ref: "#/components/schemas/Instant"
        updatedAt:
          $ref: "#/components/schemas/Instant"
        html:
          type: string
    DiscardResponse:
      type: object
      properties:
        success:
          type: boolean
    DnsSpec:
      type: object
      required:
      - domain
      - type
      - value
      properties:
        domain:
          type: string
        type:
          $ref: "#/components/schemas/ProjectDnsRecordType"
        value:
          type: string
    DomainOutcome:
      type: string
      enum:
      - REGISTERED
      - NO_MATCHING_ZONE
      - NOT_CONFIGURED
      - FAILED
    DraftDto:
      type: object
      properties:
        content:
          type: string
        serializedPrompt:
          type: string
        promptVersion:
          type: integer
          format: int64
        lastRunAt:
          $ref: "#/components/schemas/Instant"
        lastRunPromptVersion:
          type: integer
          format: int64
        lastRunCommandId:
          type: string
        updatedAt:
          $ref: "#/components/schemas/Instant"
    DraftResponse:
      type: object
      properties:
        draft:
          $ref: "#/components/schemas/DraftDto"
    Entry:
      type: object
      properties:
        feature:
          $ref: "#/components/schemas/FeatureDto"
    Entry1:
      type: object
      properties:
        task:
          $ref: "#/components/schemas/TaskDto"
    Entry2:
      type: object
      properties:
        epic:
          $ref: "#/components/schemas/EpicDto"
    Entry3:
      type: object
      properties:
        ticket:
          $ref: "#/components/schemas/TicketDto"
    Entry4:
      type: object
      properties:
        comment:
          $ref: "#/components/schemas/TicketCommentDto"
    Entry5:
      type: object
      properties:
        repository:
          $ref: "#/components/schemas/RepositoryDto"
        declared:
          type: boolean
          description: The wrapper's .gitmodules names this repository
    Entry6:
      type: object
      properties:
        path:
          type: string
        name:
          type: string
        repositoryId:
          type: string
    Entry7:
      type: object
      properties:
        project:
          $ref: "#/components/schemas/ProjectDto"
    EntryOutcome:
      type: object
      properties:
        path:
          type: string
        name:
          type: string
        repositoryId:
          type: string
        archetype:
          $ref: "#/components/schemas/RepositoryArchetype"
        component:
          type: string
        outcome:
          $ref: "#/components/schemas/Outcome"
        warning:
          type: string
    EpicDto:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        title:
          type: string
        slug:
          type: string
        status:
          type: string
        supersededByEpicId:
          type: string
        description:
          type: string
        createdAt:
          $ref: "#/components/schemas/Instant"
        updatedAt:
          $ref: "#/components/schemas/Instant"
    FeatureDto:
      type: object
      properties:
        id:
          type: string
        epicId:
          type: string
        title:
          type: string
        slug:
          type: string
        description:
          type: string
        dependsOnFeatureId:
          type: string
        implementedOn:
          $ref: "#/components/schemas/Instant"
        createdAt:
          $ref: "#/components/schemas/Instant"
        updatedAt:
          $ref: "#/components/schemas/Instant"
    Instant:
      type: string
      format: date-time
      examples:
      - 2022-03-10T16:15:50Z
    LastBackupDto:
      type: object
      properties:
        outcome:
          $ref: "#/components/schemas/BackupOutcome"
        at:
          $ref: "#/components/schemas/Instant"
        detail:
          type: string
    LaunchPin:
      type: object
      properties:
        image:
          type: string
        version:
          type: string
        launches:
          type: string
    LaunchPins:
      type: object
      properties:
        generatedAt:
          $ref: "#/components/schemas/Instant"
        pins:
          type: array
          items:
            $ref: "#/components/schemas/LaunchPin"
    ListResponse:
      type: object
      properties:
        refinements:
          type: array
          items:
            $ref: "#/components/schemas/RefinementDto"
    ListResponse1:
      type: object
      properties:
        designs:
          type: array
          items:
            $ref: "#/components/schemas/DesignDto"
    ListResponse2:
      type: object
      properties:
        attachments:
          type: array
          items:
            $ref: "#/components/schemas/AttachmentDto"
    MergeConflictDto:
      type: object
      properties:
        target:
          type: string
        conflicts:
          type: array
          items:
            $ref: "#/components/schemas/ConflictedPath"
    NewAttachment:
      type: object
      required:
      - label
      - source
      - dataBase64
      properties:
        mimeType:
          type: string
        label:
          type: string
          pattern: \S
        source:
          type: string
          pattern: \S
        dataBase64:
          type: string
          pattern: \S
    NewDesign:
      type: object
      required:
      - title
      - html
      properties:
        title:
          type: string
          pattern: \S
        html:
          type: string
          pattern: \S
        sourceRoute:
          type: string
        truncated:
          type: boolean
    OpenRequest:
      type: object
      required:
      - epicId
      properties:
        epicId:
          type: string
          pattern: \S
    Outcome:
      type: string
      enum:
      - CREATED
      - ADOPTED
      - KEPT
      - ARCHETYPE_UPDATED
      - COMPONENT_UPDATED
      - SYNC_TARGET_UPDATED
      - UNDECLARED
      - SKIPPED
    ProcessResponse:
      type: object
      properties:
        refinement:
          $ref: "#/components/schemas/RefinementDto"
        technicalProcessId:
          type: string
    ProjectDnsRecordDto:
      type: object
      properties:
        domain:
          type: string
        type:
          $ref: "#/components/schemas/ProjectDnsRecordType"
        value:
          type: string
    ProjectDnsRecordType:
      type: string
      enum:
      - A
      - AAAA
      - CNAME
    ProjectDto:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        slug:
          type: string
        description:
          type: string
        dns:
          $ref: "#/components/schemas/ProjectDnsRecordDto"
    RefinementDto:
      type: object
      properties:
        id:
          type: integer
          format: int64
        epicId:
          type: string
        projectId:
          type: string
        repositoryId:
          type: string
        branch:
          type: string
        parent:
          type: string
        label:
          type: string
        preamble:
          type: string
        runtimeStatus:
          type: string
        runtimeError:
          type: string
        clean:
          type: boolean
        ahead:
          type: integer
          format: int32
        behind:
          type: integer
          format: int32
        conflictsWithParent:
          type: boolean
        agentActivity:
          type: string
        daemonConnectedAt:
          $ref: "#/components/schemas/Instant"
        daemonVersion:
          type: string
        daemonOutdated:
          type: boolean
        createdAt:
          $ref: "#/components/schemas/Instant"
    RefinementResponse:
      type: object
      properties:
        refinement:
          $ref: "#/components/schemas/RefinementDto"
    ReleaseArtifactDto:
      type: object
      properties:
        type:
          type: string
        name:
          type: string
        version:
          type: string
    ReleaseArtifactsDto:
      type: object
      properties:
        version:
          type: string
        releasedSha:
          type: string
        deployable:
          type: boolean
        artifacts:
          type: array
          items:
            $ref: "#/components/schemas/ReleaseArtifactDto"
        detail:
          type: string
    ReleaseRequestCommitsDto:
      type: object
      properties:
        mergedSha:
          type: string
        commits:
          type: array
          items:
            $ref: "#/components/schemas/CommitDto"
        detail:
          type: string
    ReleaseRequestDto:
      type: object
      properties:
        id:
          type: string
        repoId:
          type: string
        repoName:
          type: string
        backingBranch:
          type: string
        sources:
          type: array
          items:
            $ref: "#/components/schemas/ReleaseRequestSourceDto"
        priority:
          type: string
        mergedSha:
          type: string
        state:
          type: string
        summary:
          type: string
        requester:
          type: string
        detail:
          type: string
        conflict:
          $ref: "#/components/schemas/MergeConflictDto"
        version:
          type: string
        releasedSha:
          type: string
        mergedToMainAt:
          $ref: "#/components/schemas/Instant"
        retryable:
          type: boolean
        createdAt:
          $ref: "#/components/schemas/Instant"
        updatedAt:
          $ref: "#/components/schemas/Instant"
    ReleaseRequestSourceDto:
      type: object
      properties:
        kind:
          type: string
        name:
          type: string
        ref:
          type: string
        implicit:
          type: boolean
        priority:
          type: string
    RenameDesign:
      type: object
      required:
      - title
      properties:
        title:
          type: string
          pattern: \S
    RenameRepositoryRequest:
      type: object
      required:
      - name
      properties:
        name:
          type: string
          pattern: \S
    RepositoryArchetype:
      type: string
      enum:
      - PROJECT
      - SERVICE
      - DAEMON
      - LIBRARY
      - FRONTEND
      - CLI
      - IMAGE
      - SERVICE_TEMPLATE
      - FORK
    RepositoryCoordinatesDto:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        name:
          type: string
        mainBranch:
          type: string
        archetype:
          $ref: "#/components/schemas/RepositoryArchetype"
    RepositoryDto:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        backupUrl:
          type: string
        mainBranch:
          type: string
        archetype:
          $ref: "#/components/schemas/RepositoryArchetype"
        component:
          type: string
        projectId:
          type: string
        lastBackup:
          $ref: "#/components/schemas/LastBackupDto"
    ResolveDesign:
      type: object
      required:
      - mode
      properties:
        mode:
          type: string
          pattern: \S
    Response:
      type: object
      properties:
        epic:
          $ref: "#/components/schemas/EpicDto"
    Response1:
      type: object
      properties:
        epic:
          $ref: "#/components/schemas/EpicDto"
        successor:
          $ref: "#/components/schemas/EpicDto"
    Response10:
      type: object
      properties:
        success:
          type: boolean
    Response11:
      type: object
      properties:
        task:
          $ref: "#/components/schemas/TaskDto"
    Response12:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry2"
    Response13:
      type: object
      properties:
        epic:
          $ref: "#/components/schemas/EpicDto"
    Response14:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry3"
    Response15:
      type: object
      properties:
        ticket:
          $ref: "#/components/schemas/TicketDto"
    Response16:
      type: object
      properties:
        task:
          $ref: "#/components/schemas/TaskDto"
    Response17:
      type: object
      properties:
        task:
          $ref: "#/components/schemas/TaskDto"
    Response18:
      type: object
      properties:
        success:
          type: boolean
    Response19:
      type: object
      properties:
        comment:
          $ref: "#/components/schemas/TicketCommentDto"
    Response2:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry"
    Response20:
      type: object
      properties:
        success:
          type: boolean
    Response21:
      type: object
      properties:
        ticket:
          $ref: "#/components/schemas/TicketDto"
    Response22:
      type: object
      properties:
        ticket:
          $ref: "#/components/schemas/TicketDto"
    Response23:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry4"
    Response24:
      type: object
      properties:
        ticket:
          $ref: "#/components/schemas/TicketDto"
    Response25:
      type: object
      properties:
        success:
          type: boolean
    Response26:
      type: object
      properties:
        comment:
          $ref: "#/components/schemas/TicketCommentDto"
    Response27:
      type: object
      properties:
        project:
          $ref: "#/components/schemas/ProjectDto"
    Response28:
      type: object
      properties:
        repositoryId:
          type: string
    Response29:
      type: object
      properties:
        projectId:
          type: string
        wrapperRepositoryId:
          type: string
        branch:
          type: string
        entries:
          type: array
          items:
            $ref: "#/components/schemas/EntryOutcome"
    Response3:
      type: object
      properties:
        epic:
          $ref: "#/components/schemas/EpicDto"
    Response30:
      type: object
      properties:
        domain:
          $ref: "#/components/schemas/DomainOutcome"
        domainDetail:
          type: string
    Response31:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry5"
        wrapper:
          $ref: "#/components/schemas/WrapperView"
    Response32:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry7"
    Response33:
      type: object
      properties:
        project:
          $ref: "#/components/schemas/ProjectDto"
    Response34:
      type: object
      properties:
        success:
          type: boolean
    Response35:
      type: object
      properties:
        repository:
          $ref: "#/components/schemas/RepositoryDto"
        projectId:
          type: string
        wrapperPath:
          type: string
    Response36:
      type: object
      properties:
        project:
          $ref: "#/components/schemas/ProjectDto"
        wrapper:
          $ref: "#/components/schemas/RepositoryDto"
    Response37:
      type: object
      properties:
        projectId:
          type: string
        scheduled:
          type: integer
          format: int32
    Response38:
      type: object
      properties:
        repository:
          $ref: "#/components/schemas/RepositoryDto"
        projectId:
          type: string
    Response39:
      type: object
      properties:
        requests:
          type: array
          items:
            $ref: "#/components/schemas/ReleaseRequestDto"
    Response4:
      type: object
      properties:
        success:
          type: boolean
    Response40:
      type: object
      properties:
        request:
          $ref: "#/components/schemas/ReleaseRequestDto"
    Response41:
      type: object
      properties:
        request:
          $ref: "#/components/schemas/ReleaseRequestDto"
    Response42:
      type: object
      properties:
        requests:
          type: array
          items:
            $ref: "#/components/schemas/ReleaseRequestDto"
    Response43:
      type: object
      properties:
        request:
          $ref: "#/components/schemas/ReleaseRequestDto"
    Response44:
      type: object
      properties:
        request:
          $ref: "#/components/schemas/ReleaseRequestDto"
    Response45:
      type: object
      properties:
        request:
          $ref: "#/components/schemas/ReleaseRequestDto"
    Response46:
      type: object
      properties:
        technicalProcessId:
          type: string
    Response47:
      type: object
      properties:
        repository:
          $ref: "#/components/schemas/RepositoryDto"
    Response48:
      type: object
      properties:
        repository:
          $ref: "#/components/schemas/RepositoryDto"
        previousName:
          type: string
        changed:
          type: boolean
    Response49:
      type: object
      properties:
        technicalProcessId:
          type: string
    Response5:
      type: object
      properties:
        feature:
          $ref: "#/components/schemas/FeatureDto"
    Response50:
      type: object
      properties:
        technicalProcessId:
          type: string
    Response51:
      type: object
      properties:
        repositories:
          type: array
          items:
            $ref: "#/components/schemas/RepositoryCoordinatesDto"
    Response52:
      type: object
      properties:
        repository:
          $ref: "#/components/schemas/RepositoryDto"
    Response53:
      type: object
      properties:
        success:
          type: boolean
    Response54:
      type: object
      properties:
        success:
          type: boolean
    Response55:
      type: object
      properties:
        builds:
          type: array
          items:
            $ref: "#/components/schemas/CommitBuildStatusDto"
    Response56:
      type: object
      properties:
        branches:
          type: array
          items:
            $ref: "#/components/schemas/BranchDto"
    Response57:
      type: object
      properties:
        repositoryId:
          type: string
        scheduled:
          type: boolean
    Response58:
      type: object
      properties:
        technicalProcessId:
          type: string
    Response59:
      type: object
      properties:
        dispatch:
          $ref: "#/components/schemas/TicketAgentDispatchDto"
    Response6:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/AuditEntryDto"
    Response7:
      type: object
      properties:
        feature:
          $ref: "#/components/schemas/FeatureDto"
    Response8:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry1"
    Response9:
      type: object
      properties:
        feature:
          $ref: "#/components/schemas/FeatureDto"
    SaveRequest:
      type: object
      required:
      - content
      properties:
        content:
          type: string
        serializedPrompt:
          type: string
    SetMainBranchRequest:
      type: object
      required:
      - branch
      properties:
        branch:
          type: string
          pattern: \S
    SetReleaseSourcePriority:
      type: object
      required:
      - branch
      - priority
      properties:
        branch:
          type: string
          pattern: \S
        priority:
          type: string
          pattern: \S
        requester:
          type: string
    SyncStatusDto:
      type: object
      properties:
        branch:
          type: string
        remoteReachable:
          type: boolean
        remoteExists:
          type: boolean
        ahead:
          type: integer
          format: int32
        behind:
          type: integer
          format: int32
    TaskDto:
      type: object
      properties:
        id:
          type: string
        featureId:
          type: string
        repositoryId:
          type: string
        title:
          type: string
        slug:
          type: string
        description:
          type: string
        dependsOnTaskId:
          type: string
        implementedAt:
          $ref: "#/components/schemas/Instant"
        createdAt:
          $ref: "#/components/schemas/Instant"
        updatedAt:
          $ref: "#/components/schemas/Instant"
    TechnicalProcessFrame:
      type: object
      properties:
        segment:
          type: string
        kind:
          type: string
        seq:
          type: integer
          format: int64
        line:
          type: string
        status:
          type: string
        hint:
          type: string
        hintTarget:
          type: string
    TicketAgentDispatchDto:
      type: object
      properties:
        workspaceRowId:
          type: integer
          format: int64
        repositoryId:
          type: string
        branch:
          type: string
        fresh:
          type: boolean
        agentLaunch:
          type: string
    TicketCommentDto:
      type: object
      properties:
        id:
          type: string
        ticketId:
          type: string
        author:
          type: string
        body:
          type: string
        createdAt:
          $ref: "#/components/schemas/Instant"
        updatedAt:
          $ref: "#/components/schemas/Instant"
    TicketDto:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        title:
          type: string
        slug:
          type: string
        type:
          type: string
        status:
          type: string
        assignee:
          type: string
        createdBy:
          type: string
        description:
          type: string
        createdAt:
          $ref: "#/components/schemas/Instant"
        updatedAt:
          $ref: "#/components/schemas/Instant"
    TransitionEpicRequest:
      type: object
      properties:
        target:
          type: string
    TransitionTicketRequest:
      type: object
      properties:
        target:
          type: string
    UpdateEpicRequest:
      type: object
      required:
      - title
      properties:
        title:
          type: string
          pattern: \S
        description:
          type: string
    UpdateFeatureRequest:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        dependsOnFeatureId:
          type: string
        clearDependsOn:
          type: boolean
        implementedOn:
          $ref: "#/components/schemas/Instant"
        clearImplementedOn:
          type: boolean
    UpdateProjectRequest:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
    UpdateTaskRequest:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        dependsOnTaskId:
          type: string
        clearDependsOn:
          type: boolean
        implementedAt:
          $ref: "#/components/schemas/Instant"
        clearImplementedAt:
          type: boolean
    UpdateTicketCommentRequest:
      type: object
      required:
      - body
      properties:
        body:
          type: string
          pattern: \S
    UpdateTicketRequest:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        clearDescription:
          type: boolean
        type:
          type: string
        assignee:
          type: string
        clearAssignee:
          type: boolean
    WithdrawReleaseRequest:
      type: object
      properties:
        reason:
          type: string
    WrapperView:
      type: object
      properties:
        repositoryId:
          type: string
        branch:
          type: string
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry6"
  securitySchemes:
    SecurityScheme:
      type: openIdConnect
      openIdConnectUrl: http://qits-platform-idp:8080/idp/.well-known/openid-configuration
      description: Authentication
paths:
  /projects/api/epics/{epicId}/features:
    get:
      parameters:
      - name: epicId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response2"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List Features
      tags:
      - Epic Controller
      security:
      - SecurityScheme:
        - qits:admin
    post:
      parameters:
      - name: epicId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFeatureRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response5"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Create Feature
      tags:
      - Epic Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/epics/{id}:
    put:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEpicRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Update
      tags:
      - Epic Controller
      security:
      - SecurityScheme:
        - qits:admin
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response3"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Epic Controller
      security:
      - SecurityScheme:
        - qits:admin
    delete:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response4"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Delete
      tags:
      - Epic Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/epics/{id}/audit:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response6"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Audit
      tags:
      - Epic Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/epics/{id}/transition:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TransitionEpicRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response1"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Transition
      tags:
      - Epic Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/features/{featureId}/tasks:
    get:
      parameters:
      - name: featureId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response8"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List Tasks
      tags:
      - Feature Controller
      security:
      - SecurityScheme:
        - qits:admin
    post:
      parameters:
      - name: featureId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTaskRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response11"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Create Task
      tags:
      - Feature Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/features/{id}:
    put:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateFeatureRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response7"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Update
      tags:
      - Feature Controller
      security:
      - SecurityScheme:
        - qits:admin
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response9"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Feature Controller
      security:
      - SecurityScheme:
        - qits:admin
    delete:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response10"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Delete
      tags:
      - Feature Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/pins:
    get:
      summary: The container images a launch by this service would pull right now
      responses:
        "200":
          description: "The effective launch pins, image order"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LaunchPins"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Pins Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /projects/api/projects:
    get:
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response32"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List
      tags:
      - Project Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProjectRequest"
        required: true
      responses:
        "200":
          description: "The project exists, with its wrapper repository"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response36"
        "409":
          description: The supplied slug already names another project — slugs are
            unique
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Create
      tags:
      - Project Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/projects/{id}:
    put:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProjectRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response27"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Update
      tags:
      - Project Controller
      security:
      - SecurityScheme:
        - qits:admin
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response33"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Project Controller
      security:
      - SecurityScheme:
        - qits:admin
    delete:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response34"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Delete
      tags:
      - Project Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/projects/{projectId}/agent-container:
    get:
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentContainerResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Agent Container Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/projects/{projectId}/agent-container/ensure:
    post:
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentContainerResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Ensure
      tags:
      - Agent Container Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/projects/{projectId}/agent-container/stop:
    post:
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentContainerResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Stop
      tags:
      - Agent Container Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/projects/{projectId}/epics:
    get:
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      - name: status
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response12"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List
      tags:
      - Project Epics Controller
      security:
      - SecurityScheme:
        - qits:admin
    post:
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEpicRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response13"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Create
      tags:
      - Project Epics Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/projects/{projectId}/events:
    get:
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            text/event-stream:
              schema:
                type: array
                items:
                  type: string
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Events
      tags:
      - Project Events Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/projects/{projectId}/reconcile:
    post:
      summary: Re-assert a project's domain through the dns registrar
      description: "Project creation registers the dns record fire-and-forget, so\
        \ a receiver that was down when a project was created leaves the record missing\
        \ with nothing to carry it forward. This re-asserts it synchronously and reports\
        \ the outcome; the receiver is idempotent, so it is safe to repeat, and it\
        \ is also how a project created before the hook existed gets its record. No\
        \ registrar is wired at present, so the outcome is FAILED saying so and dns\
        \ records are configured by hand at the external provider."
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: The reconcile ran. A failed re-assertion is still a 200 — the
            outcome is the result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response30"
        "404":
          description: No such project
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Project Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/projects/{projectId}/refinements:
    get:
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List
      tags:
      - Project Refinements Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/projects/{projectId}/release-requests:
    get:
      summary: "The project's release requests, across all of its repositories"
      description: "Most recently moved first. With no state the answer is the open\
        \ requests — the work still waiting on somebody — plus the last 10 released,\
        \ so that a release does not leave the page the moment it lands. Pass state=all\
        \ for the whole history, or a state name (PENDING, READY, RELEASED, REJECTED,\
        \ FAILED, CONFLICTED, WITHDRAWN) to narrow to one."
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      - name: state
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response39"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Project Release Requests Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /projects/api/projects/{projectId}/repositories:
    get:
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response31"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List Repositories
      tags:
      - Project Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
    post:
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProjectRepositoryRequest"
        required: true
      responses:
        "200":
          description: The repository exists and the wrapper names it
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response35"
        "400":
          description: "Neither or both of url/name, an unplaceable archetype, a name\
            \ that carries no role suffix when no archetype is stated, a name already\
            \ taken in the project, or a wrapper commit the git host refused"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Create Repository
      tags:
      - Project Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/projects/{projectId}/repositories/adopt:
    post:
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdoptProjectRepositoryRequest"
        required: true
      responses:
        "200":
          description: "The repository is registered under this project, with that\
            \ name as its alias"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response38"
        "400":
          description: "An id or name outside the adoptable shape, archetype PROJECT\
            \ (the wrapper's, which this route does not own), or an unusable url"
        "404":
          description: "No such project, or the git host holds no repository under\
            \ that storage id"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Adopt Repository
      tags:
      - Project Controller
      security:
      - SecurityScheme:
        - qits:system
  /projects/api/projects/{projectId}/repositories/backup-sync:
    post:
      summary: Back up every repository of this project to its forge twin
      description: "Schedules a backup per repository that has a twin, debounced per\
        \ repository. Outcomes land on each repository's lastBackup rather than in\
        \ this response."
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      responses:
        "202":
          description: Queued; the body says how many
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response37"
        "404":
          description: No such project
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Project Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/projects/{projectId}/repositories/by-name/{repoName}:
    get:
      summary: Resolve a project-scoped repository name to its id
      description: "The name is the segment a committed relative submodule url (../<name>.git)\
        \ resolves to, with any .git suffix stripped. Resolution is exactly what wrapper\
        \ membership honours: the (project, name) alias, and nothing else. The projectId\
        \ segment is the project's id OR its slug — the slug is the public spelling\
        \ of a clone url (/git/qits/qits-ci) and the id is matched first, so both\
        \ always resolve. The id answered is the opaque storage key this service and\
        \ qits-githost share, not a name."
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      - name: repoName
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: The name resolves
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response28"
        "404":
          description: "No such project, or no repository by that name"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Project Controller
      security:
      - SecurityScheme:
        - qits:system
  /projects/api/projects/{projectId}/repositories/reconcile:
    post:
      summary: Bring a project's repositories in line with its wrapper's .gitmodules
      description: "The wrapper repository is the project's configuration: every submodule\
        \ entry gets a repository (adopted when the git host already serves it, cloned\
        \ from the entry's backend otherwise), the directory an entry sits under decides\
        \ its archetype, and a repository no entry names is reported UNDECLARED and\
        \ left alone. This never deletes: a deleted repository loses its history on\
        \ the git host, so the answer names the strays and a person decides. Idempotent,\
        \ and the way a project imported from a wrapper url is materialized."
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: The reconcile ran. A per-entry failure is still a 200 — the
            outcomes are the result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response29"
        "400":
          description: The project has no wrapper repository
        "404":
          description: No such project
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Project Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/projects/{projectId}/tickets:
    get:
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      - name: status
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response14"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List
      tags:
      - Project Tickets Controller
      security:
      - SecurityScheme:
        - qits:admin
    post:
      parameters:
      - name: projectId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTicketRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response15"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Create
      tags:
      - Project Tickets Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpenRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RefinementResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Open
      tags:
      - Refinement Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RefinementResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Refinement Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}/active-process:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActiveProcessResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Active Process
      tags:
      - Refinement Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}/designs:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListResponse1"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List
      tags:
      - Refinement Design Controller
      security:
      - SecurityScheme:
        - qits:admin
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewDesign"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: {}
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Add
      tags:
      - Refinement Design Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}/designs/{designId}:
    put:
      parameters:
      - name: designId
        in: path
        required: true
        schema:
          type: string
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RenameDesign"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DesignDto"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Rename
      tags:
      - Refinement Design Controller
      security:
      - SecurityScheme:
        - qits:admin
    get:
      parameters:
      - name: designId
        in: path
        required: true
        schema:
          type: string
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DesignDto"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Refinement Design Controller
      security:
      - SecurityScheme:
        - qits:admin
    delete:
      parameters:
      - name: designId
        in: path
        required: true
        schema:
          type: string
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: No Content
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Delete
      tags:
      - Refinement Design Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}/designs/{designId}/resolve:
    post:
      parameters:
      - name: designId
        in: path
        required: true
        schema:
          type: string
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResolveDesign"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DesignDto"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Resolve
      tags:
      - Refinement Design Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}/discard:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscardResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Discard
      tags:
      - Refinement Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}/ensure-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProcessResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Ensure
      tags:
      - Refinement Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}/events:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            text/event-stream:
              schema:
                type: array
                items:
                  type: string
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Events
      tags:
      - Refinement Events Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}/prompt-attachments:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListResponse2"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List
      tags:
      - Refinement Prompt Attachment Controller
      security:
      - SecurityScheme:
        - qits:admin
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewAttachment"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: {}
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Add
      tags:
      - Refinement Prompt Attachment Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}/prompt-attachments/{attachmentId}:
    put:
      parameters:
      - name: attachmentId
        in: path
        required: true
        schema:
          type: string
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewAttachment"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttachmentDto"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Replace
      tags:
      - Refinement Prompt Attachment Controller
      security:
      - SecurityScheme:
        - qits:admin
    delete:
      parameters:
      - name: attachmentId
        in: path
        required: true
        schema:
          type: string
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: No Content
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Delete
      tags:
      - Refinement Prompt Attachment Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}/prompt-attachments/{attachmentId}/content:
    get:
      parameters:
      - name: attachmentId
        in: path
        required: true
        schema:
          type: string
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            image/png: {}
            image/jpeg: {}
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Content
      tags:
      - Refinement Prompt Attachment Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}/prompt-draft:
    put:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DraftResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Save
      tags:
      - Refinement Prompt Draft Controller
      security:
      - SecurityScheme:
        - qits:admin
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DraftResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Refinement Prompt Draft Controller
      security:
      - SecurityScheme:
        - qits:admin
    delete:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: No Content
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Delete
      tags:
      - Refinement Prompt Draft Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}/recreate-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProcessResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Recreate
      tags:
      - Refinement Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/refinements/{id}/stop-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RefinementResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Stop
      tags:
      - Refinement Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/repositories:
    get:
      summary: Every repository with its public coordinates
      description: "The machine-readable catalogue: row id, project, addressable name,\
        \ main branch and archetype, sorted by project then name. A repository that\
        \ owns no name is listed with a null name; one whose name declares no role\
        \ suffix carries a null archetype."
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response51"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /projects/api/repositories/{repoId}:
    patch:
      summary: Rename a repository
      description: "Gives the repository a new project-scoped addressable name. Nothing\
        \ moves on the git host — a bare is keyed by the repository's id — so /git/<project>/<newName>\
        \ serves it immediately and the old name stops resolving. The archetype is\
        \ re-derived from the new name's role suffix (-service, -daemon, -frontend,\
        \ -cli, -oci, -javalib, -jslib) and left as it was when the new name declares\
        \ none. The project's wrapper is NOT rewritten: update the .gitmodules entry\
        \ to the new name and push, or the repository reads as UNDECLARED until you\
        \ do. The backup twin self-heals from the wrapper on the next reconcile."
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RenameRepositoryRequest"
        required: true
      responses:
        "200":
          description: The repository answers to the new name
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response48"
        "400":
          description: "An invalid name, a name another repository in this project\
            \ already answers to, or the project's wrapper repository (whose name\
            \ is derived from the immutable slug)"
        "404":
          description: No such repository
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
    get:
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response52"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
    delete:
      summary: "Delete a repository, on the git host as well"
      description: "Deletes the row, the repository's workspaces, its local mirror,\
        \ its entry in the project's wrapper if it is still there, and the repository\
        \ itself on qits-githost. The history is gone: there is no tombstone and no\
        \ retention, and a git host that fails the delete fails the whole request,\
        \ leaving the row. The project's wrapper repository is refused — delete the\
        \ project instead."
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response54"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/repositories/{repoId}/active-process:
    get:
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response58"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Active Process
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/repositories/{repoId}/backup-sync:
    post:
      summary: Back this repository up to its forge twin now
      description: "Schedules the same backup the git host's post-receive triggers,\
        \ debounced per repository. The outcome lands on the repository's lastBackup\
        \ rather than in this response."
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      responses:
        "202":
          description: Queued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response57"
        "404":
          description: No such repository
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/repositories/{repoId}/branches:
    delete:
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      - name: branch
        in: query
        schema:
          type: string
          pattern: \S
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response53"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Delete Branch
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
    get:
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response56"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Branches
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/repositories/{repoId}/commits:
    get:
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      - name: branch
        in: query
        schema:
          type: string
          pattern: \S
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommitLogDto"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Commits
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/repositories/{repoId}/commits/{commitHash}/builds:
    get:
      summary: Every CI verdict recorded for one commit
      description: "One entry per terminal CI run of this commit, newest first, fed\
        \ from qits-ci's build events. Queued and running builds do not appear; an\
        \ empty list means no verdict yet, never that the commit is fine."
      parameters:
      - name: commitHash
        in: path
        required: true
        schema:
          type: string
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response55"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /projects/api/repositories/{repoId}/commits/{commitHash}/changes:
    get:
      parameters:
      - name: commitHash
        in: path
        required: true
        schema:
          type: string
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      - name: parent
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommitChangesDto"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Commit Changes
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/repositories/{repoId}/commits/{commitHash}/diff:
    get:
      parameters:
      - name: commitHash
        in: path
        required: true
        schema:
          type: string
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      - name: parent
        in: query
        schema:
          type: string
      - name: path
        in: query
        schema:
          type: string
          pattern: \S
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommitFileDiffDto"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Commit File Diff
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/repositories/{repoId}/main-branch:
    put:
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetMainBranchRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response47"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Set Main Branch
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/repositories/{repoId}/pull:
    post:
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response50"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Pull
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/repositories/{repoId}/push:
    post:
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response49"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Push
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/repositories/{repoId}/release-requests:
    get:
      summary: This repository's release requests
      description: "Newest first. With no state the answer is the open requests —\
        \ everything that can still move — plus the last 10 released, so that a release\
        \ does not vanish off the page the moment it lands. Pass state=all for the\
        \ whole history (WITHDRAWN included), or a state name (PENDING, READY, RELEASED,\
        \ REJECTED, FAILED, CONFLICTED, WITHDRAWN) to narrow to one."
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      - name: state
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response42"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Release Request Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
    post:
      summary: Ask for a branch to be released once its builds are green
      description: "Creates (or converges on) the open release request the branch\
        \ participates in. The request's sources are main plus the named branch, plus\
        \ every released tag of the repository not yet merged to main; they are folded\
        \ onto release/<id> and it is that MERGE the gates evaluate — mergedSha on\
        \ the answer. A branch that already participates in an open request answers\
        \ that request rather than opening a second. Poll until RELEASED, REJECTED,\
        \ CONFLICTED or FAILED; detail says why, and conflict says what to resolve."
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateReleaseRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response44"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      tags:
      - Release Request Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /projects/api/repositories/{repoId}/release-requests/{requestId}:
    get:
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      - name: requestId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response43"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Release Request Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /projects/api/repositories/{repoId}/release-requests/{requestId}/artifacts:
    get:
      summary: "What this release published, and whether anything deploys it"
      description: "Read out of the released tag's own tree: deployable is whether\
        \ it declares .config/qits/deployments.yml, and artifacts is what its release\
        \ recipe declares plus the userflow bundle its QA pipeline publishes (at the\
        \ fold's sha, not at the version). A request that has not released answers\
        \ 200 with version null and a detail saying so; a git host that cannot be\
        \ asked and a recipe that will not parse do the same. A repository that declares\
        \ no recipe published nothing, and says so with an empty list and no detail\
        \ at all."
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      - name: requestId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseArtifactsDto"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Release Request Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /projects/api/repositories/{repoId}/release-requests/{requestId}/commits:
    get:
      summary: The commits this request's fold brought in
      description: "The range mergedSha^1..mergedSha — the first parent of an octopus\
        \ merge is the branch it was folded onto, so what is left is exactly what\
        \ the request's sources contributed. It stays the same answer after the release\
        \ reaches main. The version bump is not in the list: the release commits the\
        \ rewritten manifests ON TOP of the fold. An empty list is never an error\
        \ — detail says whether nothing has been folded yet, the fold is no longer\
        \ in the repository's history, or the fold genuinely added nothing."
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      - name: requestId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseRequestCommitsDto"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Release Request Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /projects/api/repositories/{repoId}/release-requests/{requestId}/sources:
    post:
      summary: Add a branch to an open release request
      description: "The request is re-folded with the new source and, if the fold\
        \ produces a new commit, the gates are re-armed onto it. Idempotent: a branch\
        \ already on the request answers the request unchanged. A RELEASED or WITHDRAWN\
        \ request answers 409. Implicit tag sources are not addable — they are derived\
        \ from what the repository has in flight."
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      - name: requestId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddReleaseRequestSource"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response45"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      tags:
      - Release Request Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /projects/api/repositories/{repoId}/release-requests/{requestId}/sources/priority:
    post:
      summary: Re-state how urgently one of a request's branches wants to be released
      description: "Priority is per participating branch and the request answers with\
        \ the max over them, so raising one branch raises the request. Nothing is\
        \ re-folded and no event is published: the same branches are folded onto the\
        \ same backing branch, so the sha the gates are evaluating has not moved.\
        \ The value reaches the platform with the release itself, which reads the\
        \ sources live. A branch the request does not name is a 404, a word naming\
        \ no priority is a 400, and a RELEASED or WITHDRAWN request is a 409. Implicit\
        \ tag sources carry no priority and cannot be addressed here."
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      - name: requestId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetReleaseSourcePriority"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response41"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      tags:
      - Release Request Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /projects/api/repositories/{repoId}/release-requests/{requestId}/withdraw:
    post:
      summary: Withdraw an open release request
      description: "The ask is moot — nothing should land this branch. WITHDRAWN is\
        \ terminal and frees the branch: the next release ask mints a fresh request.\
        \ A request already RELEASED or WITHDRAWN answers 409. A deleted branch withdraws\
        \ its open request automatically; this route is the operator's spelling for\
        \ every other reason."
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      - name: requestId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WithdrawReleaseRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response40"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      tags:
      - Release Request Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /projects/api/repositories/{repoId}/sync:
    post:
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response46"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Sync
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/repositories/{repoId}/sync-status:
    get:
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SyncStatusDto"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Sync Status
      tags:
      - Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/tasks/{id}:
    put:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTaskRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response16"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Update
      tags:
      - Task Controller
      security:
      - SecurityScheme:
        - qits:admin
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response17"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Task Controller
      security:
      - SecurityScheme:
        - qits:admin
    delete:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response18"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Delete
      tags:
      - Task Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/technical-processes/{id}/events:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            text/event-stream:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TechnicalProcessFrame"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Events
      tags:
      - Technical Process Events Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/ticket-comments/{id}:
    put:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTicketCommentRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response19"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Update
      tags:
      - Ticket Comment Controller
      security:
      - SecurityScheme:
        - qits:admin
    delete:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response20"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Delete
      tags:
      - Ticket Comment Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/tickets/{id}:
    put:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTicketRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response21"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Update
      tags:
      - Ticket Controller
      security:
      - SecurityScheme:
        - qits:admin
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response24"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Ticket Controller
      security:
      - SecurityScheme:
        - qits:admin
    delete:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response25"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Delete
      tags:
      - Ticket Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/tickets/{id}/dispatch-agent:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response59"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Dispatch Agent
      tags:
      - Ticket Dispatch Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/tickets/{id}/transition:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TransitionTicketRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response22"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Transition
      tags:
      - Ticket Controller
      security:
      - SecurityScheme:
        - qits:admin
  /projects/api/tickets/{ticketId}/comments:
    get:
      parameters:
      - name: ticketId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response23"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List Comments
      tags:
      - Ticket Controller
      security:
      - SecurityScheme:
        - qits:admin
    post:
      parameters:
      - name: ticketId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTicketCommentRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response26"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Create Comment
      tags:
      - Ticket Controller
      security:
      - SecurityScheme:
        - qits:admin
info:
  title: qits-projects API
  version: 2026.908.81637
